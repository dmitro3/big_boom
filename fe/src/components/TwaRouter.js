import { useEffect, useMemo } from 'react';

import { PATH } from '../consts.js';
import { useNavigate } from 'react-router-dom';

export const TwaRouter = ({ children }) => {


  return children;
};
